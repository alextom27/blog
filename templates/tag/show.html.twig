{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h3>Tag</h3>
                <p>    <a href="{{ path('tag_edit', {'id': tag.id}) }}">edit</a>
                </p>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <table class="table">
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ tag.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ tag.name }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{ include('tag/_delete_form.html.twig') }}

            </div>
        </div>
    </div>







{% endblock %}
